function harteLogikFuerEchteEntwickler(angst) {
    const rage = 'useless';

    for (let arguement in ['bla', 'bla']) {
        console.log(`HTML IST KEINE PROGRAMMIERSPRACHE BLA! ${arguement}`);
    }

    return rage + angst;
}
